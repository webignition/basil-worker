version: "3"

services:
  app-web:
    build:
      context: ../..
      args:
        DATABASE_URL: postgresql://postgres:${WORKER_POSTGRES_PASSWORD}@postgres:${WORKER_POSTGRES_EXPOSED_PORT}/basil-worker-db?serverVersion=12&charset=utf8
    container_name: test-basil-worker-app-web
    depends_on:
      - postgres
      - rabbitmq
